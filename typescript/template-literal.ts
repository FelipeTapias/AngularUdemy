(function(){

    function getEdad(){
        return 100 + 100 + 300;
    }
    
    const nombre = 'Felipe';
    const apellido = 'Tapias';
    const edad = 18;

    const salida = `${ nombre }\n${ apellido }\n${getEdad()}`;
    console.log(salida);
    
})();
